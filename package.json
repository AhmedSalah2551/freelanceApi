{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch --experimental-strip-types api/index.ts",
    "vercel-dev": "node --watch --experimental-strip-types api/index.ts",
    "start": " NODE_ENV=production  node --experimental-strip-types api/index.ts",
    "build": "cd client && npm run build:client &&  npm run build:server",
    "vercel-build": "cd client && npm run build:client &&  npm run build:server"
  },
  "keywords": [],
  "engines": {
    "node": "22.12.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.2",
    "multer": "^1.4.5-lts.1"
  }
}
